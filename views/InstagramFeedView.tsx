import React, { useEffect } from 'react';
import InstagramCarusal from '../componenets/carousals/InstagramCarusal';

interface Props {}

const InstagramFeedView = (props: Props) => {
  return (
    <div>
      <InstagramCarusal />
    </div>
  );
};

export default InstagramFeedView;
